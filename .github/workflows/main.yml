on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Login to Docker hup"
        run: docker login --username rahma310 --password dckr_pat_PWYfjKEDgmTlFtrgrKCes9yPhsg



